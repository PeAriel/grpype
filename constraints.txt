# Same pins as pyproject.toml — use with:
#   pip install -e . -c constraints.txt
# to keep numpy / pandas / matplotlib on the validated stack during resolution.
numpy==1.24.3
scipy==1.11.4
pandas==2.1.0
matplotlib==3.9.4
swifttools==3.0.23
